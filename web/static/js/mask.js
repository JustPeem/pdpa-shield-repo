// Page 01 — Mask Logs.  Owner: M09
import * as api from "./api.js";

export function init() {
  // TODO(M09):
  //  [ ] source-log textarea: min 300px (220px mobile), grows with content, max 72vh, smooth height
  //  [ ] buttons: upload (+ drag & drop), generate, assignment sample, clear
  //  [ ] rule toggles (iOS switches) -> api.mask(text, enabled)
  //  [ ] debounce typing (250 ms); ignore out-of-order responses
  //  [ ] A · stats: total card, risk ring + level badge + formula popover, 5 per-type counters (count-up)
  //  [ ] B · before / after panes from `parts`; hover a highlight -> tooltip with groups + Luhn info
  //  [ ] redaction animation (bar wipes over each masked span, staggered); copy + download masked.log
  void api;
}
export function show() {}
