// SVG icon set (no emoji anywhere in the UI).  Owner: M09
// icon("shield") -> <svg class="i">…</svg>. Stroke icons on a 24×24 grid, stroke = currentColor.
export const ICONS = {
  // TODO(M09): shield, eye, eyeoff, lock, code, checksq, graph, upload, shuffle, file, trash,
  //            copy, download, play, pause, step, reset, check, x, info, moon, sun, terminal
};
export function icon(name) {
  // TODO(M09)
  throw new Error(`icon ${name} not implemented`);
}
