## What

<!-- One or two sentences. -->

Closes #

## Checklist

- [ ] Only Python `re` used for matching (no other regex library, no JS regex)
- [ ] New/changed patterns use `re.VERBOSE` with a comment on each line
- [ ] Added or updated cases (including at least one "must NOT match")
- [ ] `pytest -q` and `ruff check .` pass locally
- [ ] UI changes: checked TH + EN, light + dark, 390px width (attach a screenshot)
- [ ] Touched a shared contract? Linked the `contract` issue and tagged the affected owners
